{% extends 'base.html.twig' %}

{% block title %}AnnonceEmploi{% endblock %}

{% block body %}
    <h1>AnnonceEmploi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ annonce_emploi.id }}</td>
            </tr>
            <tr>
                <th>Job</th>
                <td>{{ annonce_emploi.job }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ annonce_emploi.description }}</td>
            </tr>
            <tr>
                <th>Localization</th>
                <td>{{ annonce_emploi.localization }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ annonce_emploi.createdAt ? annonce_emploi.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_annonce_emploi_index') }}">back to list</a>

    <a href="{{ path('app_admin_annonce_emploi_edit', {'id': annonce_emploi.id}) }}">edit</a>

    {{ include('admin_annonce_emploi/_delete_form.html.twig') }}
{% endblock %}
