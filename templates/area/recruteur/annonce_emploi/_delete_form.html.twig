<form method="post" action="{{ path('app_annonce_emploi_delete', {'id': annonce_emploi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce_emploi.id) }}">
    <button class="btn">Supprimer l'annonce</button>
</form>
