{% extends 'base.html.twig' %}

{% block title %}AnnonceEmploi{% endblock %}

{% block body %}
    <h1>AnnonceEmploi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Poste recherché</th>
                <td>{{ annonce_emploi.job }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ annonce_emploi.description }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ annonce_emploi.localization }}</td>
            </tr>
            <tr>
                <th>Création de l'annonce</th>
                <td>{{ annonce_emploi.createdAt ? annonce_emploi.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annonce_emploi_index') }}">retour à votre liste d'annonce</a>

    <a href="{{ path('app_annonce_emploi_edit', {'id': annonce_emploi.id}) }}">modifier</a>

    {{ include('area/recruteur/annonce_emploi/_delete_form.html.twig') }}
{% endblock %}
