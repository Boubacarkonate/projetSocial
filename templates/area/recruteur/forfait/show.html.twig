{% extends 'base.html.twig' %}

{% block title %}Forfait{% endblock %}

{% block body %}
    <h1>Forfait</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forfait.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ forfait.name }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ forfait.duration }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ forfait.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ forfait.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_forfait_index') }}">retour à la liste</a>
    
{% endblock %}
