{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                
                <td>{{ commentaire.createdAt ? commentaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                
                <td>{{ commentaire.job }}</td>
            </tr>
            <tr>
                
                <td>{{ commentaire.description }}</td>
            </tr>
            <tr>
                
                <td>{{ commentaire.user.name }} {{ commentaire.user.firstname }}</td>
            </tr>
        </tbody>
    </table>
{{ dump() }}
    <a href="{{ path('app_admin_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_admin_commentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('admin/admin_commentaire/_delete_form.html.twig') }}
{% endblock %}
